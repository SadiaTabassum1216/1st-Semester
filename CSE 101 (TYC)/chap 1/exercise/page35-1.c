#include<stdio.h>
void outnum(int num);

int main()
{
int i,a;
    printf("take a number: ");
    scanf("%d",i);

        printf("the number is %d",a);

    return 0;
}

void outnum(int num)
{
        printf("%d",num);
}

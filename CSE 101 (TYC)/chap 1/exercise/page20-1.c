#include<stdio.h>

int main(void)
{
int len,width,height;

        printf("enter length: ");
        scanf("%d", &len);


        printf("enter width: ");
        scanf("%d", &width);


        printf("enter height: ");
        scanf("%d", &height);

            printf("area is %d",len*width*height);

    return 0;
}
